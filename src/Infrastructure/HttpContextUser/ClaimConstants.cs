namespace Roller.Infrastructure.HttpContextUser;

public static class ClaimConstants
{
    public const string RoleId = nameof(RoleId);

    public const string PermissionCode = nameof(PermissionCode);
}